import StyledSkeleton from './Skeleton';

function Skeleton({ width, height, marginBottom }) {
    return <StyledSkeleton width={width} height={height} marginBottom={marginBottom} />;
}

export default Skeleton;